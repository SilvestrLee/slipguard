Read the required SGOS context and inspect the repository.

Then confirm scope, non-scope, dependencies, acceptance criteria, affected files, and the smallest implementation plan. Implement incrementally, run tests, update tasks and changelog, and report the outcome and remaining risks.
