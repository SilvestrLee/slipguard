# Engineering Debt Register — Sprint E-06C Validation

**Stage:** 10 of 12 · **Date:** 2026-07-25

Consolidated from Stages 1–9's Category B and C findings only. Category D (product/roadmap observations — AV-2 presentation-layer criterion untestable pending U-02, PI-2 the already-ADR-tracked re-analysis gap, SEC-1 `SlipAnalysisPolicy` awaiting a real route) are **excluded** per this stage's instruction and are instead carried into `production-readiness.md` as observations for Product/Architecture Office, not engineering debt.

| ID | Title | Description | Risk | Priority | Recommended Sprint | Effort | Owner | Categories | Source |
|---|---|---|---|---|---|---|---|---|---|
| ED-001 | Normalization layer's Eloquent coupling | `NormalizeBettingSlip` (Domain/Normalization) directly type-hints and reads `App\Models\BettingSlip` (`->status`, `->legs`), giving a domain layer real framework knowledge instead of accepting a plain DTO/array. | Low — no incorrect behaviour, fully test-covered; purely a boundary-purity concern. | Medium | Next engineering-focused sprint (not urgent) | S (a few hours) | Engineering | Refactoring, Maintainability | AV-1 |
| ED-002 | Cascade-delete undermines analysis immutability guarantee | `slip_analyses`/`leg_analyses` foreign keys use `cascadeOnDelete()`. A hard delete of a `BettingSlip`/`User` would silently destroy historical analysis records ADR-007 calls immutable. Currently unreachable via any customer path (`BettingSlipPolicy::delete()` only allows Draft/Ready deletion, never Analysed), but the schema provides no independent guarantee. | Medium — dormant today, becomes real the moment an admin-deletion or account-deletion feature ships without this being revisited. | High (before any deletion feature) | Before any account/admin-deletion feature; otherwise no later than E-08 MVP Hardening | S (schema change + migration) | Engineering, pending Architecture Office decision on deletion semantics | Maintainability, Observability | DB-1 |
| ED-003 | No static type-checking tool configured | Neither PHPStan nor Psalm is installed. A `BigDecimal`/enum-heavy deterministic engine would benefit from compile-time type guarantees beyond what Pest's runtime tests catch. | Low | Low | Next tooling sprint | M (install + baseline + fix initial findings) | Engineering | Automation, Developer Experience | SA-1 |
| ED-004 | No architecture-boundary enforcement tool | ADR-007's forbidden call flow (Risk Engine must never touch persistence/HTTP/UI) is enforced only by code review and grep-based manual audit (this sprint's Stage 2), not a build-time tool like Deptrac. | Low today given the boundary currently holds; risk grows as the codebase and team grow. | Low | Future tooling sprint, revisit once U-02 adds a presentation layer to actually police | M | Engineering | Automation | SA-2 |
| ED-005 | Local Herd PHP toolchain broken, undocumented | The Herd-linked `php`/`php83` binaries fail at process start (`dyld` symbol error). `/usr/local/bin/php` (Homebrew) works and satisfies `composer.json`. Undocumented anywhere — a future contributor (or CI) hitting the same binary would lose time diagnosing it. | Low (workaround exists, this machine only) | Low | Whenever convenient | XS (one-line note) | Engineering | Developer Experience | SA-3 |
| ED-006 | Stale sprint-scratch documents at repo root | `next-step.md`, `report_now.md`, `U-01-REPORT.md` live at repo root instead of `docs/`, are superseded by `CHANGELOG.md`/`TASKS.md`, and (for `U-01-REPORT.md`) contain a now-false closing statement ("nothing committed yet"). Confirmed independently by both Stage 1 and Stage 9. | Low — no correctness risk, only reader confusion. | Low | Trivial housekeeping, any time; founder should confirm deletion vs. archival first | XS | Engineering (pending founder confirmation — do not delete without sign-off) | Maintainability, Developer Experience | IA-1, DOC-1 |
| ED-007 | Duplicated text-normalization helpers | `mb_strtolower(trim())`/whitespace-collapse logic hand-rolled 3× across `NormalizeSport`/`NormalizeFootballMarket`. Already identified and deliberately deferred in the E-05A code review (`CHANGELOG.md:163`); re-confirmed present, unchanged status. | Low — correct, tested; duplication only. | Low | Any future normalization-layer sprint | S | Engineering | Refactoring | IA-2 |
| ED-008 | Taxonomy rebuilt on every lookup; lifecycle-status display duplicated | `FootballMarketTaxonomyV1`'s definition array is rebuilt per lookup (no caching); `BettingSlipStatus` display rules are duplicated across policy and views instead of centralized on the enum. Already identified and deliberately deferred in E-05A; re-confirmed present. | Low at current data volume/traffic. | Low | Any future sprint touching either area | S | Engineering | Performance, Refactoring | IA-3 |
| ED-009 | No secondary index on `slip_analyses.user_id` / composite index on `leg_analyses(slip_analysis_id, display_order)` | Laravel's `constrained()` creates FK constraints but not redundant secondary indexes; `SlipAnalysis::legAnalyses()`'s `orderBy('display_order')` has no supporting composite index. Immaterial at ≤20 rows/parent and single-user-scoped queries. | Low today; relevant only if History ever queries `slip_analyses` at scale across users. | Low | Revisit only if/when a scaled History query pattern appears | XS | Engineering | Performance | DB-2, DB-3 |
| ED-010 | Unused `updated_at` column on immutable tables | `slip_analyses`/`leg_analyses` both carry `timestamps()`, so `updated_at` exists but is structurally guaranteed to never be written. A `created_at`-only schema would make the immutability guarantee visible in the schema itself. | None — cosmetic. | Low | Any future migration touching these tables | XS | Engineering | Maintainability | PI-1 |
| ED-011 | `SlipAnalysisPolicy` relies on implicit deny rather than explicit `create`/`update`/`delete` methods | Correctness currently depends on Laravel's "no method ⇒ denied" default plus the fact nothing calls those abilities. An explicit `return false` (with a comment) would make the immutability intent self-documenting in code, not just by omission. | Low today (nothing reachable calls it). | Low | Once a real route/use case forces the policy to be touched again | XS | Engineering | Maintainability, Observability | SEC-2 |
| ED-012 | No convention preventing ad hoc report files at repo root | `U-01-REPORT.md` is the only instance found, but nothing structurally prevents a second one from appearing outside `docs/`. | Low | Low | Only if it recurs | XS | Engineering | Developer Experience, Automation | DOC-2 |
| ED-013 | `LegAnalysis` rows inserted one-by-one instead of batched | `AnalyzeBettingSlip::execute()`'s per-leg `foreach` calls `LegAnalysis::create()` individually (up to 20 inserts per analysis) instead of a single batched `insert()`. Confirmed linear-but-avoidable overhead via this sprint's benchmark (Stage 6): ~1 query and ~0.5ms added per leg. | Low at current scale (20-leg ceiling, ~11.5ms median end-to-end). | Low | Revisit only if leg-count ceiling rises materially or analysis becomes high-frequency/bulk | S | Engineering | Performance | PB-1 |
| ED-014 | No formally recorded end-to-end (persist-inclusive) performance target | Only the pure-engine sub-millisecond target is documented (`RISK_RULE_SET_2026_1.md` §13); no SLA exists for the full normalize+engine+persist pipeline measured in this sprint's benchmark. | Low — current numbers are comfortably fast regardless. | Low | Once U-02 (Report screen) makes a real HTTP-request-path benchmark possible | XS (documentation) | Engineering + Product Office | Observability | PB-2 |
| ED-015 | Unmodified Laravel/Pest scaffold example tests | `tests/Unit/ExampleTest.php` and `tests/Feature/ExampleTest.php` protect no SlipGuard-specific behaviour (`expect(true)->toBeTrue()`; a bare `/` 200-check). Harmless noise in an otherwise strong, exact-value-assertion-driven suite. | None. | Low | Any time | XS | Engineering | Maintainability | TR-1 |
| ED-016 | Test factories rely on unseeded random defaults | `BettingSlipLegFactory`'s default `sport`/`decimal_odds` use unseeded Faker randomness. Every determinism-sensitive test today correctly overrides these explicitly, so there is no current flakiness — flagged only as a guardrail note for future test authors. | None today; latent risk only if a future test omits the override. | Low | N/A — informational, no action required now | — | Engineering | Developer Experience | TR-2 |

## Summary

- **0** items requiring urgent action before certification.
- **2** Category B items (ED-001, ED-002) — genuine but currently-dormant/low-risk; ED-002 is the one item worth flagging explicitly to Architecture Office before any future deletion-related feature.
- **14** Category C items — hygiene, tooling gaps, and micro-optimizations, none blocking, all appropriately low-priority per the sprint's "prefer stability over elegance" directive.
- Several items (IA-2, IA-3) are **not new** — they are re-confirmations of debt already knowingly deferred in E-05A's own code review, correctly still deferred.

No item in this register changes the Stage 11/12 production-readiness recommendation.
